const INITIAL_STATE = {

}
const reducer = (state = INITIAL_STATE, action) => {
const {payload }=action
    switch (action.type) {

        

        default:
            return state
        }
}
export default reducer